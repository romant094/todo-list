import React from 'react';

const AppHeader = () => {
    return (
        <div>
            <h1>ToDo list</h1>
            <span> {1} more to do, {3} done</span>
        </div>
    );
}

export default AppHeader;